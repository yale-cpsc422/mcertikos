#include <proc.h>
#include <stdio.h>
#include <syscall.h>

int
main(int argc, char **argv)
{
  /*
  uint32_t ball = 1;

  while (1)
  {
    if (sys_ssend(3, ball++) == E_IPC)
      printf("Error occured in ding\n");
    sys_yield();
  }
  */
  return 0;
}
